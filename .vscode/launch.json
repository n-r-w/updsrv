{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/updsrv.go",
            "args": ["-config-path", "./config.toml"],
            "cwd": "${workspaceRoot}"
        }
    ]
}